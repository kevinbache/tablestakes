import React, {useState} from 'react';
import PropTypes from 'prop-types';

const Page = props => {

  return (
    <div className={"page"}>
      Page
    </div>
  );
};

Page.propTypes = {

};

export default Page;